# Xvfb Configuration
# Used when Weston/Wayland is not available

# Display settings
DISPLAY_NUM=1
SCREEN_NUM=0
WIDTH=1920
HEIGHT=1080
DEPTH=24

# Xvfb options
XVFB_OPTS="-ac -nolisten tcp -dpi 96"

# X11VNC options
X11VNC_OPTS="-forever -shared -noxdamage -noxfixes -noxrandr"
